# Enforce Unix line endings for shell scripts
*.sh text eol=lf
*.bash text eol=lf

# Enforce Unix line endings for Python
*.py text eol=lf

# Enforce Windows line endings for PowerShell (if needed)
*.ps1 text eol=crlf

# Auto-detect for everything else
* text=auto
